{
  "installCommand": "corepack yarn install",
  "buildCommand": "corepack yarn workspace frontend build",
  "outputDirectory": "frontend/dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}